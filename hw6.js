//1. Намалювати трикутник
/*function drawTriangle(height, symbol) {
    for (let i = 1; i <= height; i++) {
        let row = '';
        for (let j = 1; j <= i; j++) {
            row += symbol;
        }
        console.log(row);
    }
}

drawTriangle(5, '*');
*/

// 2. Вивести на екран суму чисел від 1 до 100 включно, які не кратні 3.
/*let sum = 0;

for (let i = 1; i <= 100; i++) {
    if (i % 3 !== 0) {
        sum += i;
    }
}
*/

//3. Написати функцію pow(x,y) яка буде приймати 2 цисла, перше це число
// яке ми будемо зводити у ступінь,друге у яку ступінь.
// Наприклад pow(2,3) функція поверне значення 8.
/*function pow(x, y) {
    let result = 1;

    for (let i = 0; i < y; i++) {
        result *= x;
    }

    return result;
}

let result = pow(2, 3);
console.log(result);

 */





